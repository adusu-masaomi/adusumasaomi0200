<%= simple_form_for @unit_master, :html => { :class => 'well form-horizontal' } do |f| %>
  <%= f.input :unit_name %>
  
  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                unit_masters_path, :class => 'btn btn-default' %>
<% end %>
