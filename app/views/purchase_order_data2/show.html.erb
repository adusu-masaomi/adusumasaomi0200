<%- model_class = PurchaseOrderDatum -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:purchase_order_code) %>:</strong></dt>
  <dd><%= @purchase_order_datum.purchase_order_code %></dd>
  <dt><strong><%= model_class.human_attribute_name(:construction_datum_id) %>:</strong></dt>
  <dd><%= @purchase_order_datum.construction_datum_id %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              purchase_order_data2_index_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_purchase_order_data2_path(@purchase_order_datum), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              purchase_order_data2_index_path(@purchase_order_datum),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
