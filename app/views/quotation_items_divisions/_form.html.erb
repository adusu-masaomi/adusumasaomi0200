<%= simple_form_for(@quotation_items_division) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :quotation_items_division_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
